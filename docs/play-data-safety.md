# Google Play 数据安全准备

- 数据收集：否。
- 数据共享：否。
- 账号创建：不支持。
- 数据传输加密：应用不向开发者服务器传输数据。
- 删除请求：全部数据位于本机，可在应用内删除或清除应用数据。
- 文件访问：仅使用 Android 系统文件选择器读取用户明确选择的文件。
- 第三方 SDK：当前无广告、分析或崩溃收集 SDK。
- 隐私政策 URL：发布前需把 `docs/privacy-policy.md` 托管到公开 HTTPS 地址。

提交 Play Console 前，应根据最终依赖和实际发布包重新核对以上声明。
